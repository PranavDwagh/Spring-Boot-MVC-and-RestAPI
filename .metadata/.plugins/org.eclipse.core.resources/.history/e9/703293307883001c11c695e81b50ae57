<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.js"></script>
    <script>
        $(document).ready(()=>{
            $("#b1").click(()=>{
                $.ajax({
                    url:'/grocery/getAll',
                    type:"GET",
                    success:function(data)
                    {
                        var t = $(`<table></table>`)
                       // var obj = JSON.stringify(data)
                        for(var i=0; i<data.length;i++)
                        {
                            // grocery/add/{id}/{name}/{cost}/{description}/{unit}/{date1}
                            t.append(`<td><tr> ${data[i].id}</tr></td> <td><tr>${data[i].name}</tr></td> <td><tr>${data[i].description}</tr></td> <td><tr>${data[i].unit}</tr></td> <td><tr>${data[i].date}</tr></td> `)
                        }
                        $("#dis").append(t);
                    }
                })
            })

            $("#b2").click(()=>{

                $.ajax({
                    url:'/grocery/getAll',
                    type:"GET",
                    success:function(data)
                    {
                        $("#dis").html('');
                        var s = $("<select></select>");
                        for(var i=0; i<data.length;i++)
                        {
                           // console.log(data[i].name)
                            s.append(`<option value=${data[i].name}>${data[i].name}</option>`)
                        }
                        $("#dis").append(s);
                    }
                })
            })
            // grocery/add/{id}/{name}/{cost}/{description}/{unit}/{date1}
            $("#b3").click(()=>{
                var id = $("#id").val();
                var name = $("#name").val();
                var cost = $("#cost").val();
                var description = $("#description").val();
                var unit = $("#unit").val();
                var date11 = $("#date").val();

                var url = `grocery/add/${id}/${name}/${cost}/${description}/${unit}/${date11}`

                $.ajax({
                    url:url,
                    type:'POST',
                    success:function(data)
                    {
                        $("#dis").html("<b>Inserted</b>");
                    }
                })
            })
        })        
    </script>

</head>
<body>
    <button id="b1">Get All Books</button>
    <button id="b2">Get All Books in DropDown</button>
    <button id="b3">Insert product</button>
    ID:<input type="number" name="" id="id"/>
    Book Name:<input type="text" name="" id="name"/>
    Cost :<input type="number" name="" id="cost"/>
    Book description:<input type="description" name="" id="desc"/>
    Unit:<input type="unit" name="" id="desc"/>
    Date :<input type="date" name="" id="date"/>

    <p id="dis"></p>
</body>
</html>